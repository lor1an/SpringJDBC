CREATE TABLE Genres(
   ID INTEGER IDENTITY not null,
   name varchar(30),
 );

CREATE TABLE Authors (
    ID INTEGER IDENTITY,
    Name VARCHAR (30) NOT NULL,
    Surname VARCHAR(30) NOT NULL,
);
